<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rgvms.mapper.MisMapper">

	<!-- mis 게시물 등록 -->
	<select id="insert" resultType="com.rgvms.domain.MisVO">
		INSERT INTO TBL_MIS(misNo, times, title)
		VALUES (seq_mis.nextval, ? , ?)
	</select>

	<!-- mis 게시물 리스트  조회 -->
	<select id="list">
	<![CDATA[
		SELECT
		misNo, times, title, misRegdate
		FROM
		TBL_MIS
		WHERE misNo > 0
		ORDER BY misNo DESC misRegdate DESC
	]]>
	</select>

	<!-- mis 검색할 때 전체 카운트 가져오기 -->
	<select id="listSearchCount">


	</select>

	<!-- mis 게시물 삭제 -->
	<delete id="delete">
		DELETE FROM TBL_MIS
		WHERE misNo = #{misNo}
	</delete>
	
	<!-- mis 상세 조회 -->
	<select id="select">
		SELECT
		times, title
		FROM
		TBL_MIS
		WHERE misNo = #{misNo}
	</select>

	<!-- mis 수정 -->
	<select id="update">
		UPDATE TBL_MIS
		SET times = #{times}, title = #{title}
		WHERE misNo = #{misNo}
	</select>

</mapper>