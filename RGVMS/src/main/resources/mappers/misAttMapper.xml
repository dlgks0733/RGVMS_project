<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rgvms.mapper.MisAttMapper">

	<!-- mis 출결 리스트 조회  -->
	<select id="List" resultType="com.rgvms.domain.MisAttVO">
	<![CDATA[
		SELECT
			MIS_ATT_NO, 
			MIS_ATT_DATE, 
			USER_NO, 
			MIS_NO
		FROM
			TBL_MIS_ATT
		
	]]>
	</select>
	
	<!-- mis 출결 등록 -->
	<insert id="insert">
		INSERT INTO TBL_MIS_ATT
			(MIS_ATT_NO, MIS_ATT_DATE, USER_NO, MIS_NO)
		VALUES 
			(SEQ_MIS_ATT.NEXTVAL, #{misAttDate} , #{userNo}, #{misNo})
	</insert>
	
	
	<!-- mis 출결 학생 리스트 조회  -->
	<select id="stuList" resultType="com.rgvms.domain.UserVO">
	<![CDATA[
		SELECT
			USER_NO,
			GRADE,
			USER_NAME
		FROM
			TBL_USER
	
	]]>
	</select>
	
	<!-- mis 출결 삭제(학생 삭제) -->
	<delete id="delete">
		DELETE FROM TBL_MIS_ATT
		WHERE MIS_ATT_NO = #{misAttNo}
	</delete>
	
	<!-- mis 내 점수 상세 조회 -->
	<select id="myMisList">
		SELECT
			ROWNUM, 
			M.TIMES, 
			M.TITLE, 
			A.MIS_ATT_DATE
		FROM
			TBL_MIS M, 
			TBL_MIS_ATT A
		WHERE M.MIS_NO = #{A.misNo}
	</select>
	
</mapper>