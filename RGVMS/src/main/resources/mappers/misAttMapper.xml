<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rgvms.mapper.MisAttMapper">

	<!-- mis 출결 등록 -->
	<select id="insert" resultType="com.rgvms.domain.MisAttVO">
		INSERT INTO TBL_MIS(misAttNo,
		misAttDate, userNo, misNo)
		VALUES (seq_mis_Att.nextval, ? , ?, ?)
	</select>

	<!-- mis 출결 리스트 조회  -->
	<select id="List">
	<![CDATA[
		SELECT
		misAttNo, misAttDate, userNo, misNo
		FROM
		TBL_MIS_ATT
		WHERE misAttNo > 0
		ORDER BY misAttNo DESC misAttDate DESC
	]]>
	</select>
	
	<!-- mis 출결 학생 리스트 조회  -->
	<select id="stuList">
	<![CDATA[
		SELECT
		misAttNo, misAttDate, userNo, misNo
		FROM
		TBL_MIS_ATT
		WHERE misAttNo > 0
		ORDER BY misAttNo DESC misAttDate DESC
	]]>
	</select>
	
	<!-- mis 출결 삭제(학생 삭제) -->
	<delete id="deleteAtt">
		DELETE FROM TBL_MIS_ATT
		WHERE misAttNo = #{misAttNo}
	</delete>
	
	<!-- mis 내 점수 상세 조회 -->
	<select id="myMisList">
		SELECT
		rowNum, m.times, m.title, a.misAttDate
		FROM
		TBL_MIS m, TBL_MIS_ATT a
		WHERE m.misNo = #{a.misNo}
	</select>
	
</mapper>